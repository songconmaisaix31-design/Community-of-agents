# 真实功能接入轮

## 2026-09-14：全量回归与可用性验收

用户要求全量测试、可用性测试、接口测试；此前“本机免密 SSH，网页保留登录鉴权”继续执行。源码基线 `c1bbb32da585e65508fd9709193131d9ce7e4ec5`，沿用原 C、Kimi K3、I 三轨，不重写平台。共享 DTO、身份、幂等与演示/真实约定不变。

| 原轨 | 本轮互斥写域 / 交付 | 依赖与验收 |
| --- | --- | --- |
| C / gongzhi-core | 原 Core 写域、tests/core、docs/core、根配置与生成客户端；当前 Node 全套、真实 PG/Auth、REST/MCP 和构建 | 普通合入基线；新建独立测试环境，建议 3079 / 56640 / 56641，先查占用；历史隔离守卫只能显式参数化且保留安全约束，不对旧体验库执行写入测试 |
| K / gongzhi-kimi-adaptation | 原 public/community（排除生成客户端）、tests/frontend/evomap*、docs/frontend/evomap-adaptation.md；当前前端桌面/移动/键盘与流程测试 | 原 Kimi K3 先做 fixture 回归，再消费 C 的独立测试账号和服务；不输出秘密，不以 fixture 证明真实身份 |
| I / gongzhi-integration | 原集成写域、独占 ECS 与已授权本机专用 SSH 配置；两轨完成后普通合并、最终验收 | 先完成已派 SSH 任务；网页鉴权不变，真实 API 拒绝匿名写入；服务器仅只读回归，生产和旧预览数据保留 |

M 仅本页与独立验收。各轨复现后窄修，小步 commit + push；跨域问题交回原 owner（Connect 问题回 D）。测试采用现有命令/配置，逐项记录真实通过、失败、跳过和退役不适用，Hugo 旧界面不恢复。禁止复用既有测试账号做破坏验收；新测试数据标记为测试，不能冒充实际 Agent 对话。公网标准域名仍需实测，备案拦截不通过其他端口/代理/隧道绕过；不启用模型、知乎或邮件收费服务。

## 2026-09-14：已有 Agent 接入与页面同步

用户要求用 MCP、curl 等接入已有 Agent，并参考 EvoMap 布局同步前端。基线 `253124f3972035f5daa2be331754cd9eb818f777`；保留 Next/Crier、现有静态页面和知乎配色，不恢复 Hugo、不另建消息或 Agent 框架。先交可复制的 Agent 指引、MCP 配置与 curl 流程，复用人类有限 grant、自登记、公开公告、回复和成果契约；不把无密钥公开读取标为已绑定 Agent，不开放匿名写入。

最小接入约定：沿用 `/mcp`、`/agent-skill.md`、`POST /api/gongzhi/agents/register` 及原交流 API；C 增补公开 `GET /api/gongzhi/connect` 发现信息和受 Agent key 保护的 `GET /api/gongzhi/agents/me` / MCP `agent_status` 身份核验，并先交唯一 DTO/客户端方法。身份与 scopes 均由服务端推导，状态响应不含密钥。D/K 在 C 交接前只依赖既有契约；新增路径不是新的消息协议。Agent key 仅宿主秘密存储注入 Bearer，首次发钥不进入通用模型上下文；未知写入结果保留原幂等键，撤销、过期、真实失败仍失败。

| 长期轨 | 互斥 write_paths | 验收 |
| --- | --- | --- |
| C / gongzhi-core | `lib/mcp.ts`、`app/mcp/**`、`app/api/gongzhi/**`（不改 runs 路由）、`lib/gongzhi/*`（不改 agent/zhihu 子目录）、`tests/core/**`、`docs/core/**`；唯一根配置/锁文件/生成客户端 owner | 先提交共享 DTO；发现、身份核验、MCP 标准客户端及现有授权/幂等/撤销检查；无新认证框架或迁移 |
| D / gongzhi-connect | `examples/agent/**`、`docs/connect/**`、`tests/connect/**` | 无本仓安装的 curl/HTTP 与 MCP 指引，CLI status 与实际回执；秘密不回显、不自动重试写入 |
| K / 原 Kimi / gongzhi-kimi-adaptation | `public/community/**` 排除 `assets/gongzhi-client.js`、`tests/frontend/evomap*`、`docs/frontend/evomap-adaptation.md` | 首页接入入口、接入页方式切换/复制/连接状态、授权与真实回执衔接；桌面/手机实测，图和现有公告操作保留 |
| I / gongzhi-integration | `docs/integration/**`、`tests/integration/**`、少量入口胶水；唯一 ECS 更新 owner | 原分支普通合并；真实隔离 Auth/PG、curl/MCP 与网页同记录验证、构建；更新现有域名测试服务并 push |

M 仅本页决策、协调和独立验收。使用已配置 Orca 长期 Agent/worktree/branch；各轨先在干净工作树普通合入基线，小步 commit+push，跨域由原 owner 返修。源码、集成、隔离真实协议测试与云端接入证据分别报告；不为云端验收伪造账号或讨论，不启用付费模型/知乎/SMTP，不增加费用或权限。用户正在测试的旧预览与数据保留，部署前核对变更和可回滚镜像。

2026-09-14 16:21 CST 独立复验：域名标准 HTTP 80 返回 `403 Forbidden`、`Server: Beaver` 和 `Non-compliance ICP Filing` 页面，HTTPS 443 握手被中断。此前域名成功记录属于历史验收；本轮继续隔离接入验证和服务器回环更新，保留数据与正常域名配置，不通过其他端口、代理或隧道绕过拦截。公网体验受备案拦截限制，不能宣称当前域名可用。

## 2026-09-14：域名测试配置

用户最新要求“域名进行部署”，并补充“先配置上，我正在进行测试”，授权为既有 ECS 配置 `zhihu.davidwang.space` 的正常 A 记录、现有 HTTPS 代理及必要的 TCP 80/443 网站入站规则。本轮从 `074d1d07ecea76d3e8ef506a4e5640045db4c052` 继续，I 独占对应云端配置和 `docs/integration/**`、`tests/integration/**`，M 只维护决定并独立复验。复用已部署镜像、卷与密钥，不重新初始化数据；不修改其他域名、IAM、SSH、付费资源或开放数据库端口。已知域名未备案，正常配置及测试可以继续，若平台拦截则记录实际结果，不以替代端口、隧道或代理绕过；最终分别报告 DNS、证书、HTTP/API 可达性并 commit + push。本节取代下方历史记录中对域名及必要网站端口配置的暂缓要求，备案和新增费用仍未授权。

## 2026-09-14：继续实际 ECS 部署

用户最新要求“解决部署的问题，自己找方法进行部署”，因此从已推送并本地验收的 `83f2fd57a5063f42e744b63a6830f8d21a12393e` 继续实际服务器部署。I 复用原 Agent/worktree/`integration/gongzhi-mvp`，独占目标 ECS 的传输、私有配置、新生产卷、显式迁移与回环服务，以及 `tests/integration/**`、`docs/integration/**`；M 只读调查传输渠道并独立复验。需要领域返修时回 C 原写域，不另开冲突轨。优先复用已验收的固定镜像，通过可信渠道传输并核对镜像 ID；验收要求真实 ECS 服务健康、TLS/迁移/只读页面均通过，不能用本机容器代替。备案、DNS/公网启用、新费用与权限扩大继续暂缓；旧数据库和预览不动。最终 commit + push，记录可体验入口和实际剩余限制。

## 2026-09-14：阿里云部署增量

用户已指定购买的北京 Ubuntu 24.04 ECS，并要求通过 `aliyun` CLI 管理，网站域名为 `zhihu.davidwang.space`。本轮源码基线 `b5ac1cd5677e97f331066a96d8ffcf5ef9289be7`；保留现有 Next/Crier、官方 GoTrue/PostgreSQL 和前端。既有 CLI 授权已实测可读取目标实例并通过云助手执行只读检查，无需另索取 SSH 密码。旧文档中“云资源尚未授权”仅为历史状态。

用户最新要求“先不做备案，先做适配”：优先交付和验收当前项目的部署适配代码。已开始的主机准备收尾，接收 Core 提交后由 I 集成并验证本地 Linux 构建及隔离的真实 TLS/Auth；不继续办理备案、续费、Workbench 接入或公网启用。服务器安装命令已返回成功，镜像仓库连通性与实际应用部署另行记录，不用 Docker 已启动冒充网站已部署。

| 原长期轨 | 唯一写域与交付 | 依赖与验收 |
| --- | --- | --- |
| C / Core | 新增 `infra/production/**`、原 Core 配置/脚本/测试/文档写域；生产 Compose、真实数据库 TLS、服务端生成私有配置、显式迁移 | 从上述基线普通合并；保留失败关闭及迁移防护，适用检查后小步 commit + push |
| I / Integration | `docs/integration/**` 与目标 ECS 的唯一远程写入；通过云助手准备 Docker/Compose、检查官方来源可达性 | 与 C 并行准备；接收提交后唯一 `merge --no-ff`，再验真实云环境，不用本地通过替代 |
| M / 总控 | `docs/source/**` 决定与验收；目标子域名 DNS、云资源范围核对 | 只处理指定域名和实例；新增费用、扩大权限、破坏操作仍交用户决定 |

最小部署约定：生产数据独立，不复制本地测试账号/公告，不删除当前共用 Auth 的体验数据库；业务数据库/Auth 不直接暴露公网；域名统一为上述 HTTPS 地址；缺 SMTP/模型配置时如实保留限制，不开放自动确认注册或伪造模型成功。构建及普通启动不隐式运行迁移。凭据只保存在服务器私有配置，禁止写入 Git、云助手命令内容或回显。

目前目标 4 核 8GiB、20GiB 系统盘、1Mbps；只读预检可用磁盘约 16GiB，未检测到 Docker 或网站/数据库服务。暂不扩容或更改带宽。用户已确认域名尚未备案；先完成服务器内部部署检查，不启用公网网站或生效解析。备案由用户提交资料与核验。此前知乎鉴权搜索和回答摘要已真实成功，详见集成基线报告；不追加调用消耗或把 API 成功等同完整 Agent 执行。

最新优先级：用户随后要求“先测试知乎”。D 优先实测 0.7.2 文档明确无需鉴权的官方活动接口（最多两个列表与一个实际条目详情），不拿它替代鉴权搜索/回答摘要验收；后两者仍缺本项目 Access Secret。C 暂停启动新隔离环境、账号/授权与 Agent 写入，只收尾已开始的配置准备片。原不同 owner 的实际互助流程后置，未执行；不删除或修改当前体验库。已完成代码片仍按原写域检查、提交、集成。

## 继续执行：不同验收账号下的实际 Agent 互助

用户要求“开始执行”，从已推送的 `7b4a105d67b154b08a1a5a4c9d3647a62d87c838` 继续。先复用现有官方 GoTrue / PostgreSQL 和 CLI，在独立本机环境验证两个不同 Auth 验收账号下的两名实际 Orca Agent；这能验证不同 owner 的产品行为，不代表已经有两名真实公众用户。没有知乎/模型配置及额度前不调用外部服务；当前业务与 Auth 共库的删除范围仍待明确。

| 轨道 | 本轮交付与写域 | 验收 |
| --- | --- | --- |
| C / 原 Core | 原 Core 写域；复用本机 Auth 配置工具，必要时补最小端口/项目隔离配置，在新独立配置、卷、端口启动验收实例；管理验收账号/有限授权并作为 Agent B 参与 | 绝不修改 3039/3043、56520/56521 的记录和配置；真实 GoTrue 身份、不同 owner、有限 scopes、真实回执，非自签用户会话 |
| D / 原 Connect | 原 Connect 写域与仅其自己的新私有 Agent 凭据；核对接入契约并作为 Agent A 自登记、发布真实评审求助、读取 B 回复并回应 | 本次自己读取和撰写，不用预写双人对白；CLI/MCP/网页同记录，不以结果提交代替任务验证 |
| I / 原 Integration | 两轨完成后唯一 no-ff 集成与验收；原 I 写域 | 两个不同验收 owner 的节点、公开交流边、权限边界与回传，真实服务和模拟检查分别报告，最终 push |

业务 DTO 和授权模型不变，不新增调度器或认证框架。配置/卷/端口先检查不存在冲突，不覆盖任何既有配置，不恢复体验库示例；只有新隔离验收环境可写测试账号和实际 Agent 记录。能力失败或范围不够时如实拒绝，Agent 不自行扩权。

用户补充的官方 API 依据更新为 `zhihu-cli-skill` 版本 `0.7.2-beta.20260911131715`；仅只读其文档，未运行安装脚本或读取 CLI 凭据。D 原轨从 `8c456c951e00cf56eb683a5791ff120313359100` 续做最小回答摘要接入：沿用 `zhihu_search` 与共享 Source，将官方 `question_answers` 接到既有平台助手，所有知乎读取共用原每次运行最多 2 次调用预算，分页只按官方游标；前端继续消费同一来源结构。OAuth、本人全文/评论、画像推荐、活动内容分别按官方授权边界保留为未接范围，不把包内能力当作本站已上线，也不自动替換现有 Auth。没有本项目凭据和额度时只验隔离 HTTP / 模型响应，不发起受凭据保护的真实调用。此增量不改变写域；D 与 K 完成后再由 I 统一集成。

## 最新增量：知乎原生 Agent 与当前数据清理

用户最新定义：以知乎的问题、经验和讨论作为重要信源，让不同人的 Agent 相互求助，把经验用于真实任务，再留下可复用成果。本轮从 `bc091b9ac7f853232120102b8b8323f64eb92f90` 出发，保留当前页面与 Next/Crier、既有授权和数据结构；不重建平台，不把知乎作者自动登记成 Agent，也不把知乎重要信源等同于已取得官方平台身份或接口权限。

| 原长期轨 | 本轮写域及交付 | 验收 |
| --- | --- | --- |
| Kimi K3 / gongzhi-kimi-adaptation | public/community（仍排除 gongzhi-client.js）、tests/frontend/evomap*、docs/frontend/evomap-adaptation.md；首页/接入/公告/来源与空状态按最新定义适配 | 原风格与实际操作保留，知乎来源、原文/摘要和互助成果清楚可见；空库不补假记录 |
| D / gongzhi-connect | lib/gongzhi/agent、lib/gongzhi/zhihu、examples/agent、tests/connect、docs/connect；复用已有工具与 CLI/MCP，调整取材、互助、回传和经验复用指引 | 当前授权范围不变；引用实际来源；未配置/失败不假成功，不以两名同所有者 Agent 冒称不同用户互助 |
| C / gongzhi-core | 原 Core 写域与本项目数据库运维；先核对精确清理目标、表/外键/保留项，主控收到用户明确范围后才执行破坏操作 | 当前 gongzhi 业务与 Auth 共库，不能将删库等同仅清业务；不触碰 Core 测试库、旧56406或其他项目；不回填验收记录 |
| I / integration/gongzhi-mvp | 原集成写域；全部增量完成后普通 no-ff 合并，少量装配与最终验收 | build/typecheck/适用回归、3039真实空状态、来源与身份边界，commit+push |

本轮共享约定沿用现有 Source / Need / Experience / Result / Owner 及 CLI/MCP，不新增消息平台、布局引擎或数据库框架。知乎真实调用仍需本项目配置及额度；未配置时只验工具边界并如实显示。数据清理范围已向用户确认，答复前只读预检；用户明确清理后不得由验收脚本重新制造公告、Agent、授权或成果。

基线 `cda811d23ed154ddf87c1a200d3cf30ffa5e7340`。依据 integration-v3 的真实帮助链及 corrections 的有限授权、Agent-only 和交流证据要求；Hugo 与旧页面要求已被用户后续选择覆盖。当前 8123 原件与留档只读，复用已验收的 3029 页面设计，不重建平台。

用户后续要求继续开发，以正式部署上线为目标。C 的根配置所有权包含最小生产 Dockerfile / .dockerignore / 启动和环境配置，I 验证生产构建、健康检查、持久化、同源入口并写部署步骤；不引入新部署框架。具体服务器/托管平台、域名、项目凭据与调用额度已询问，未提供前先完成可审阅的部署产物，不向未知环境发布、不扩大公网暴露。用户另说“修复一下”，具体故障待其说明，已知联调问题由原轨继续修复。

| 长期轨/分支 | 交付与写域摘要 | 依赖/验收 |
| --- | --- | --- |
| C / songconmaisaix31-design/gongzhi-core | Crier/持久化/Supabase Auth、共享 DTO 与客户端；根依赖和配置唯一 owner；最小本地 Auth 配置 | 首片冻结公共浏览器桥与配置接口；真实 Auth/PG 验证登记授权、撤销、身份和权限，无自写认证替代 |
| D / songconmaisaix31-design/gongzhi-connect | 复用现有 CLI/MCP/AI SDK/知乎薄适配，补真实运行缺口 | 使用 C 契约；免档案接入、发现和交流、回传/未知状态；不自造消息或 Agent 执行框架 |
| K / songconmaisaix31-design/gongzhi-kimi-adaptation | 当前页面的登录、有限授权管理、公告写入/线程回复、需求与结果/采纳、助手实际回执 | 只消费 C 公共客户端；保留风格、图引擎、双入口、镜头；桌面/手机操作与错误真实可见 |
| I / integration/gongzhi-mvp | 统一集成分支、少量入口装配、端到端验证 | 普通 no-ff merge；真实 PG/Auth、网页/CLI/MCP 同记录；最终 build/typecheck/适用测试、commit+push |

最小约定沿用 lib/gongzhi/contracts.ts 与 api-client.ts，HTTP `/api/gongzhi`，speaker/owner/scope 均由服务端决定；正文、版本、撤销、幂等和 unknown 语义不削弱。C 首片交接静态页面如何消费已存在的 Supabase SDK/公共 API 客户端，K 不复制认证/数据框架。无新增依赖确实需要则不加。

研究只看上游官方代码/文档：Crier 已锁定导入，核对差异不整仓升级；Supabase Auth 可独立自托管，优先最小本机服务；AI SDK 沿现有工具循环和预算。查到可用能力后直接用于上述缺口，不写长篇调研。

当前本机环境：已建立独立官方 GoTrue 与持久 PostgreSQL，端口 56521 / 56520；体验数据库和 Core 测试数据库分离，配置仅保留在 Git 外的本项目私有目录。原数据库与 8123 / 3019 / 3029 服务保留。生产托管、域名、云身份邮件、模型/知乎配置及调用额度仍未提供，不借用其他项目秘密。

真实验收进度：C / D 两名原长期 Agent 各用有限授权完成免档案登记，实际读取、独立回复并回读同一公告线程 `3egLqSKA`；星图仅两个 Agent 节点，交流边分别引用 `MQ3zwSss` / `V6JcqbQ2`。I 在旧集成片完成一次人类角色采纳 `UQi87nTD`，主控独立确认一条采纳记录；最终 K 返修片应验证重读，不重复首次采纳。Core / Connect 已提交推送并保留原 Agent，K 收尾未知结果重试与账号隔离，I 继续最终统一镜像及持久化验收。本机实际交流不等于平台模型或公网生产部署通过。

验收按层报告：构建与自动化；真实本机 Auth/PG/REST/MCP；两名实际 Agent 的新交流和回读；外部模型/知乎调用另列。不得把 HTTP fixture、脚本发帖或模型文字当真实执行成功。每片 commit+push，交接仅 SHA、改动、检查、限制，原 owner 持续返修。最终保留可体验本机服务，公网部署另行授权。
