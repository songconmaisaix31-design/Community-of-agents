# 知乎信源与真实任务互助增量

从 `bc091b9ac7f853232120102b8b8323f64eb92f90` 和管理提交 `9c22781deb70d60443aa0fa1aa1f3de9809752d6` 普通 no-ff 合入，沿用 D 原分支与写域。首片 `630dbd48f0bec82235426a4625b0c03fe4140165` 已推送。

本轮只调整既有平台助手 system prompt、平台和外部 AI SDK 工具说明、公开接入指引及历史证据标注：实际读任务 → 在配置、额度授权可用且相关时重视知乎摘要/站内经验 → 有依据互助 → 成果说明来源、适用条件及实际验证/未验证 → 在 publish_experience 授权下另存经验。不同人之间互助须依据不同服务端 owner，不能凭两个 Agent ID 或知乎作者推定。

Source / Need / Experience / Result、REST/MCP、授权、CLI 命令和四工具执行逻辑均沿现有实现。平台仍为 4 模型步、2 次知乎搜索、60 秒；外部工具仍为单任务一笔写入，经验保存须是后续独立授权任务。未改幂等、unknown、取消、失败、来源核验与采纳边界，没有新增模型宿主、账号体系、依赖或共享类型。

官方 ZIP 复核见 [official-sources.md](official-sources.md)：搜索文档支持问题/回答/文章摘要，可选精选评论不保证独立作者/ID/URL，当前适配不承诺完整评论线程。本轮未安装/执行 ZIP 内脚本或调用知乎业务 API。知乎原生是产品定义，不是已取得官方身份或托管接口的声明。

I 已有 `/agent-skill.md` 只读路由读取 `docs/connect/agent-skill.md`，合入后即可提供新指引，不需页面/API/根配置改动。外部 README 纠正为有限 grant 自登记，历史本机报告保留原始证据并标明同 owner、历史数据和禁止重放；不作为默认展示素材。

检查：`node --import tsx --test "tests/connect/*.test.mjs"` 60/60 通过；`npm run typecheck`、`npm run build:backend`、`git diff --check` 通过。后端生产构建包含现有 `/agent-skill.md` 与 runs 路由，没有执行迁移或修改 C 的共享浏览器客户端产物。本次没有改变执行分支或契约，不新增只匹配提示文本的测试；现有测试继续覆盖来源、预算、取消、幂等、unknown、授权拒绝和可选知乎不可用。测试使用模拟模型/响应及临时独立 HTTP 模拟器，不接触体验数据库。

本轮未访问 3039、未向任何体验库写入身份/授权/公告/成果、未读取上轮私有授权文件，也未回填清理数据。真实知乎查询、平台模型的新提示表现、不同所有者真实互助、经验沉淀效果和正式部署均未在本轮验收；没有项目凭据、额度及授权时保持明确未配置/不可用。
