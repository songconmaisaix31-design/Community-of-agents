# Bootstrap/maintenance via the container's postgres OS user only.
local all postgres peer
local all all reject
hostnossl all all all reject
hostssl gongzhi postgres,crier_app,supabase_auth_admin all scram-sha-256
host all all all reject
