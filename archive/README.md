# 本地历史归档

## 2026-09-15：旧 EvoMap 静态快照

位置：`archive/local/evomap-snapshot-20260915/`（相对仓库根目录）。

- 原始位置：`C:/Users/DW/orca/Community-of-agents/` 下未纳入 Git 的旧站点文件。
- 范围：`_next/`、`agents/`、`api/`、`brand/`、`docs/`（旧图片资源）、`evox/`、`tools/`、`zh/`，以及旧 README、Python 服务器、Windows 启动脚本、主题与根级图标等，共 19 个顶层条目。
- 文件数：1,141；总大小：73,328,582 字节。移动前后相对路径和每个文件大小一致。
- 归档原因：当前开发与实际部署使用独立工作树中的 Next/Crier 应用；旧快照已被替代。整理前未发现 8123 端口监听或该 Python 服务器进程。
- 处理方式：同工作区内原样移动，没有删除或压缩；`LICENSE`、`.git`、已有开发工作树及其未提交内容均保留原位。
- 本地归档目录被 `.gitignore` 排除，不随克隆下载，也不随 push 上传。它不是异地备份。

原目录结构完整保留，历史说明见归档内 `README.md`。此前绝对指向旧根目录的文件路径需改为归档路径。

## 查看或恢复

直接打开归档目录即可查看。若需要运行旧参考站点，从归档目录执行原 `启动本地站点.bat`，或：

```powershell
Set-Location archive/local/evomap-snapshot-20260915
python serve.py 8123
```

原服务器仍保留历史反向代理行为；这里只保留原件，没有将它改造成当前应用。当前开发请使用集成工作树。

如需恢复到旧位置，先停止使用该归档目录的本地服务，再把所需条目从归档移回仓库根目录。恢复前逐项确认目的路径不存在；根目录现在有新的导航 `README.md`，不要覆盖它，可将旧说明另存为 `README.legacy.md`。不使用覆盖移动或删除现有文件。

## 本次未归档的内容

当前 Core、Connect、前端、集成、协调、知乎方法及韩国迁移工作树全部保留。前端工作树仍有未提交修改与测试输出；其他工作树也可能包含独有提交或历史验收证据，未逐项确认退役前不移动。没有删除分支、修改公共历史、清理依赖目录、数据库或服务器文件。
